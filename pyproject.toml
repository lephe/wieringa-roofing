[project]
name = "wieringa-roofing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pycairo>=1.28.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
